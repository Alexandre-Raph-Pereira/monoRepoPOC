{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm i && lerna exec npm install --concurrency 1 -- ls -la",
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare": "husky install && husky install packages/subFolder1 && husky install packages/subFolder2"
  },
  "devDependencies": {
    "lerna": "^5.5.2"
  },
  "dependencies": {
    "chalk": "^5.0.1"
  }
}
